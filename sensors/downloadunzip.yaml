---
class_name: "DownloadSensor"
entry_point: "downloadunzip.py"
description: "Download and unzip GeoIP2 db if changed"
poll_interval: 28800
trigger_types:
  -
    name: "download_unzip"
    description: "path to downloaded files, date and response code"
    payload_schema:
      type: "object"        